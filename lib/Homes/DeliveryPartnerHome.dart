import 'package:flutter/material.dart';

class DeliveryPartnerHome extends StatelessWidget {
  const DeliveryPartnerHome({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("DPHome"),),
      body: Text("DPHome"),
    );
  }
}
